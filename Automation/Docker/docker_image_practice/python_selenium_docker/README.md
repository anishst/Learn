## Testing Python Selenium Image
1.  Build image: ```docker build -t my-python .```
2. Run container: ```docker run -it --privileged my-python ```
    -   ```--privileged``` allows print outputs to be shown